openapi: 3.1.0
info:
  title: NotebookLM REST API
  version: 3.1.2
  description: |
    Local HTTP REST API for Google NotebookLM. Citation-backed Q&A, Studio
    content generation (audio, video, infographic, report, presentation,
    data table), notebook library management, multi-account auto-reauth.

    Companion to the MCP server in the same package
    (`@roomi-fields/notebooklm-mcp`). See
    https://roomi-fields.github.io/notebooklm-mcp/ for the full guide.
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT
  contact:
    name: Romain Peyrichou
    url: https://github.com/roomi-fields/notebooklm-mcp

servers:
  - url: http://localhost:3000
    description: Local server (default)
  - url: http://{host}:{port}
    description: Custom host
    variables:
      host:
        default: localhost
      port:
        default: '3000'

tags:
  - name: Health
  - name: Auth
  - name: Q&A
  - name: Notebooks
  - name: Sources
  - name: Content
  - name: Sessions

paths:
  /:
    get:
      tags: [Health]
      summary: Server identity, version, and a pointer to the docs
      responses:
        '200': { description: Server descriptor }

  /health:
    get:
      tags: [Health]
      summary: Server health check
      responses:
        '200':
          description: Server status, uptime, active sessions
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Health' }

  /setup-auth:
    post:
      tags: [Auth]
      summary: First-time Google authentication (opens visible browser)
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                show_browser: { type: boolean, default: true }
                account: { type: string }
      responses:
        '200':
          description: Auth completed
          content: { application/json: { schema: { $ref: '#/components/schemas/Result' } } }

  /de-auth:
    post:
      tags: [Auth]
      summary: Logout, clear all credentials
      responses:
        '200':
          description: Logged out
          content: { application/json: { schema: { $ref: '#/components/schemas/Result' } } }

  /re-auth:
    post:
      tags: [Auth]
      summary: Re-authenticate or switch account
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                account: { type: string }
      responses:
        '200':
          description: Re-authenticated
          content: { application/json: { schema: { $ref: '#/components/schemas/Result' } } }

  /ask:
    post:
      tags: [Q&A]
      summary: Ask a question with citation-backed response
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/AskRequest' }
      responses:
        '200':
          description: Answer with citations
          content: { application/json: { schema: { $ref: '#/components/schemas/AskResponse' } } }
        '400':
          description: Missing question
        '500':
          description: Server error

  /notebooks:
    get:
      tags: [Notebooks]
      summary: List all notebooks in the local library
      responses:
        '200': { description: Notebook list }
    post:
      tags: [Notebooks]
      summary: Manually add a notebook to the library
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required: [id, name, url]
              properties:
                id: { type: string }
                name: { type: string }
                url: { type: string }
                description: { type: string }
                topics: { type: array, items: { type: string } }
      responses:
        '200': { description: Notebook added }

  /notebooks/scrape:
    get:
      tags: [Notebooks]
      summary: Scrape all notebooks from NotebookLM UI
      responses:
        '200': { description: Live notebook list with ids and names }

  /notebooks/import-from-scrape:
    post:
      tags: [Notebooks]
      summary: Bulk import scraped notebooks into local library
      responses:
        '200': { description: Import summary }

  /notebooks/auto-discover:
    post:
      tags: [Notebooks]
      summary: Generate notebook metadata autonomously via NotebookLM queries
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                notebook_id: { type: string }
                notebook_url: { type: string }
      responses:
        '200': { description: Generated metadata }

  /notebooks/search:
    get:
      tags: [Notebooks]
      summary: Search the local library by keyword
      parameters:
        - in: query
          name: q
          required: true
          schema: { type: string }
      responses:
        '200': { description: Matching notebooks }

  /notebooks/stats:
    get:
      tags: [Notebooks]
      summary: Library statistics
      responses:
        '200': { description: Counts and aggregates }

  /notebooks/bulk-delete:
    delete:
      tags: [Notebooks]
      summary: Delete multiple notebooks at once
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                ids: { type: array, items: { type: string } }
      responses:
        '200': { description: Deletion summary }

  /notebooks/{id}:
    get:
      tags: [Notebooks]
      summary: Get notebook details
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      responses:
        '200': { description: Notebook details }
    put:
      tags: [Notebooks]
      summary: Update notebook metadata
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      responses:
        '200': { description: Updated }
    delete:
      tags: [Notebooks]
      summary: Delete a notebook from the library
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      responses:
        '200': { description: Deleted }

  /notebooks/{id}/activate:
    put:
      tags: [Notebooks]
      summary: Set notebook as the default active notebook
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      responses:
        '200': { description: Activated }

  /notebooks/create:
    post:
      tags: [Notebooks]
      summary: Create a new notebook in NotebookLM
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                description: { type: string }
      responses:
        '200': { description: Created notebook }

  /sessions:
    get:
      tags: [Sessions]
      summary: List active sessions
      responses:
        '200': { description: Active sessions }

  /sessions/{id}:
    delete:
      tags: [Sessions]
      summary: Close a session
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      responses:
        '200': { description: Closed }

  /sessions/{id}/reset:
    post:
      tags: [Sessions]
      summary: Reset a session's history
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      responses:
        '200': { description: Reset }

  /content/sources:
    post:
      tags: [Sources]
      summary: Add a source (file, url, text, youtube, drive) to the active notebook
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/AddSourceRequest' }
      responses:
        '200': { description: Source added }
    delete:
      tags: [Sources]
      summary: Delete source by name (query parameter)
      parameters:
        - in: query
          name: name
          required: true
          schema: { type: string }
      responses:
        '200': { description: Deleted }

  /content/sources/{id}:
    delete:
      tags: [Sources]
      summary: Delete source by id
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      responses:
        '200': { description: Deleted }

  /notebooks/{id}/sources:
    get:
      tags: [Sources]
      summary: List a notebook's sources with their ids and titles
      description: |
        RPC-backed (no browser). Returns `{ notebookId, count, sources: [{ id, title }] }`.
        This is where the source ids for `/notebooks/{id}/sources/{sourceId}` come from.
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      responses:
        '200': { description: Source list }

  /notebooks/{id}/sources/{sourceId}:
    get:
      tags: [Sources]
      summary: Read a source's indexed text, one page at a time
      description: |
        RPC-backed (no browser). Returns the exact content NotebookLM reasons
        over. Sources are long — the first PDF measured ran to 157,716
        characters — so the response is paginated by default and carries
        `totalChars`, `pageChars`, `offset`, `hasMore`, `nextCursor` and a
        plain-language `continue` instruction. Pages are cut at the nearest
        line break (tag close, in HTML), and concatenating every page
        reproduces the document exactly. Pass `paginate=false` for the whole
        document in one response. The `html` rendition preserves structure but
        inlines images as base64 and is bigger.
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
        - in: path
          name: sourceId
          required: true
          schema: { type: string }
        - in: query
          name: format
          required: false
          schema: { type: string, enum: [text, html], default: text }
        - in: query
          name: paginate
          required: false
          description: Set to `false` to return the whole document in one response.
          schema: { type: boolean, default: true }
        - in: query
          name: max_chars
          required: false
          description: Page size in characters. Ignored when `paginate=false`.
          schema: { type: integer, default: 20000, minimum: 1 }
        - in: query
          name: cursor
          required: false
          description: Resume point — the `nextCursor` returned by the previous page.
          schema: { type: integer, minimum: 0 }
      responses:
        '200':
          description: One page of the source's content, plus how to fetch the rest

  /notebooks/{id}/share:
    get:
      tags: [Notebooks]
      summary: Read a notebook's sharing status
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      responses:
        '200': { description: Sharing status }
    put:
      tags: [Notebooks]
      summary: Toggle the public link
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                public: { type: boolean }
              required: [public]
      responses:
        '200': { description: Sharing status after the change }

  /notebooks/{id}/labels:
    get:
      tags: [Notebooks]
      summary: List a notebook's source labels
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      responses:
        '200': { description: Label list }
    post:
      tags: [Notebooks]
      summary: Create or delete source labels
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                action: { type: string, enum: [list, create, delete] }
                name: { type: string }
                emoji: { type: string }
                label_ids: { type: array, items: { type: string } }
      responses:
        '200': { description: Labels after the change }

  /content/study-aid:
    post:
      tags: [Content]
      summary: Generate a study aid (flashcards or quiz)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                notebook_id: { type: string }
                notebook_url: { type: string }
                kind: { type: string, enum: [flashcards, quiz] }
                focus: { type: string }
              required: [kind]
      responses:
        '200': { description: Generation result }

  /content/mind-map:
    post:
      tags: [Content]
      summary: Generate and save a mind map
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                notebook_id: { type: string }
                notebook_url: { type: string }
                title: { type: string }
      responses:
        '200': { description: Generation result }

  /content/research:
    post:
      tags: [Content]
      summary: Discover web sources for a notebook (Fast Research), optionally importing them
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                notebook_id: { type: string }
                notebook_url: { type: string }
                query: { type: string }
                import: { type: boolean }
              required: [query]
      responses:
        '200': { description: Discovered sources }

  /content/generate:
    post:
      tags: [Content]
      summary: Generate Studio content (audio, video, infographic, report, presentation, data table)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/GenerateRequest' }
      responses:
        '200': { description: Generated artifact metadata }

  /content/download:
    get:
      tags: [Content]
      summary: Download a generated artifact (WAV, MP4, PNG)
      parameters:
        - in: query
          name: content_id
          required: true
          schema: { type: string }
      responses:
        '200': { description: Binary file }

  /content:
    get:
      tags: [Content]
      summary: List sources and generated content for the active notebook
      responses:
        '200': { description: Content list }

  /content/notes:
    post:
      tags: [Content]
      summary: Create a note in the active notebook
      responses:
        '200': { description: Note created }

  /content/chat-to-note:
    post:
      tags: [Content]
      summary: Save the current chat discussion as a note
      responses:
        '200': { description: Note saved }

  /content/notes/{noteTitle}/to-source:
    post:
      tags: [Content]
      summary: Convert a note into a NotebookLM source
      parameters:
        - in: path
          name: noteTitle
          required: true
          schema: { type: string }
      responses:
        '200': { description: Note converted to source }

  /batch-to-vault:
    post:
      tags: [Q&A]
      summary: Run a batch of questions and write each answer to a vault directory
      description: |
        Long-running: one NotebookLM answer per question, written as a markdown
        file conforming to the `nblm-answer-v1` schema. Size the client timeout
        accordingly (and the reverse-proxy read timeout, if any).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                questions: { type: array, items: { type: string } }
                notebook_id: { type: string }
                notebook_url: { type: string }
                vault_dir: { type: string }
                slug_prefix: { type: string }
                source_format: { type: string, enum: [none, inline, footnotes, json, expanded] }
                sleep_between_ms: { type: integer }
                session_id: { type: string }
              required: [questions]
      responses:
        '200': { description: Batch result, one entry per question }

  /content/notes/list:
    post:
      tags: [Content]
      summary: List the notebook's notes
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                notebook_url: { type: string }
                session_id: { type: string }
      responses:
        '200': { description: Note list }

  /content/notes/get:
    post:
      tags: [Content]
      summary: Read one note's content, by title or id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                note_title: { type: string }
                note_id: { type: string }
                notebook_url: { type: string }
                session_id: { type: string }
      responses:
        '200': { description: Note content }
        '400': { description: Neither note_title nor note_id was given }

  /cleanup-data:
    post:
      tags: [Auth]
      summary: Wipe all local data (requires confirmation)
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                confirm: { type: boolean }
      responses:
        '200': { description: Cleaned }

components:
  schemas:
    Health:
      type: object
      properties:
        status: { type: string, example: ok }
        uptime_s: { type: number }
        active_sessions: { type: integer }
        version: { type: string, example: 1.5.9 }

    Result:
      type: object
      properties:
        success: { type: boolean }
        message: { type: string }
        error: { type: string }

    AskRequest:
      type: object
      required: [question]
      properties:
        question:
          type: string
          example: 'Summarize chapter 3'
        notebook_id:
          type: string
        notebook_url:
          type: string
        session_id:
          type: string
        source_format:
          type: string
          enum: [none, inline, footnotes, json, expanded]
          default: json
        show_browser:
          type: boolean
          default: false

    AskResponse:
      type: object
      properties:
        success: { type: boolean }
        answer: { type: string }
        citations:
          type: array
          items:
            type: object
            properties:
              id: { type: integer }
              source: { type: string }
              excerpt: { type: string }
        session_id: { type: string }

    AddSourceRequest:
      type: object
      required: [source_type]
      properties:
        source_type:
          type: string
          enum: [file, url, text, youtube, drive]
        file_path: { type: string }
        url: { type: string }
        text: { type: string }
        title: { type: string }
        notebook_url: { type: string }
        session_id: { type: string }

    GenerateRequest:
      type: object
      required: [content_type]
      properties:
        content_type:
          type: string
          enum:
            - audio_overview
            - video
            - infographic
            - report
            - presentation
            - data_table
        language: { type: string, example: en }
        custom_instructions: { type: string }
        video_style:
          type: string
          enum: [classroom, documentary, animated, corporate, cinematic, minimalist]
        video_format:
          type: string
          enum: [brief, explainer]
        infographic_format:
          type: string
          enum: [horizontal, vertical]
        report_format:
          type: string
          enum: [summary, detailed]
        presentation_style:
          type: string
          enum: [overview, detailed]
        presentation_length:
          type: string
          enum: [short, medium, long]
